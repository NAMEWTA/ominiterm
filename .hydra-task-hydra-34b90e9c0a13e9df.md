# Hydra Sub-Agent Task

You are working in an isolated git worktree.

- Worktree: /Users/zzzz/termcanvas/.worktrees/hydra-34b90e9c0a13e9df
- Branch: hydra/hydra-34b90e9c0a13e9df
- Base branch: main

## Task

Investigate and fix: terminal status stays 'waiting' instead of 'completed' for Claude Code sub-agents spawned via hydra.

FOCUS AREA: pollSessionId and session ID capture in src/terminal/TerminalTile.tsx

The bug: When a Claude Code agent finishes its task, the terminal status sometimes remains 'waiting' instead of transitioning to 'completed'. One possible cause: the sessionId is never captured, so session.watch() is never called.

Investigation steps:
1. Read src/terminal/TerminalTile.tsx thoroughly, especially the pollSessionId call (line 239-252) and the shell detection path (line 302-315)
2. Find the pollSessionId function definition and understand how it works
3. Add console.log instrumentation to trace:
   - When pollSessionId starts and for which terminal/ptyId
   - Each poll attempt and its result
   - When a sessionId is successfully captured
   - When session.watch() is called with what arguments
   - If pollSessionId times out or fails silently
4. Check: for hydra-spawned terminals, is the terminal type correctly set to 'claude'? Or does it go through the shell -> claude detection path which might race with the agent completing?
5. Check: does the worktreePath passed to session.watch match the path used by resolveSessionFile in session-watcher.ts?
6. Add logging with prefix [SessionCapture] for easy filtering

Write clean, minimal code. Do not over-engineer.

## Rules

- Stay within this worktree. Do not modify files outside it.
- Commit your changes before finishing.
- Do not push to remote.
- Before finishing, write `.hydra-result-hydra-34b90e9c0a13e9df.md` in the worktree root with:
  - Files changed and why
  - Issues found (if audit/review task)
  - Whether tests pass
  - Any unresolved problems
