Files changed and why

- `electron/insights-engine.ts`
  - Unified session scanning across Claude Code and Codex regardless of analyzer CLI.
  - Added time-decay tier selection, adaptive batching, prompt language sampling, stronger self-insight filtering, and section-specific analysis contexts.
  - Replaced the old memorable-moment round with the new `codingStory` round and removed hard facet/load caps.

- `electron/insights-shared.ts`
  - Expanded shared types for facet-backed counts, metrics-only counts, daily trends, tool comparison, achievements, and the new coding story section.
  - Fixed aggregation so session-level metrics come from all eligible sessions while facet-only stats remain facet-backed.
  - Added achievement computation and stronger self-insight detection.

- `electron/insights-report.ts`
  - Rebuilt the report HTML/CSS for the Insights V2 structure and dark visual design.
  - Added header count pills, coverage copy updates, 14-day time trends, tool comparison cards, achievement wall, and memorable moments rendering.
  - Kept the self-contained report and existing copy-to-clipboard behavior for prompt boxes.

- `tests/insights-engine.test.ts`
  - Added regression coverage for metrics-only aggregation, new trend/comparison/achievement stats, stronger self-insight detection, and the V2 report structure.
  - Updated existing aggregate and deterministic-summary fixtures to the new stats shape.

Issues found

- The requested design doc path `docs/plans/2026-03-22-insights-v2-design.md` was not present in this worktree. Implementation was based on the explicit task requirements instead.

Whether tests pass

- `npm test -- --grep insights`: passing
- `npm exec tsc -- --noEmit --pretty false --incremental false`: passing

Unresolved problems

- None found during implementation and verification.
