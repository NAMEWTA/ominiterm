# Hydra Result

## Task: Fix focus management race conditions, jitter, and focus loss

## Files Changed

### src/components/ComposerBar.tsx
- **Step 1**: Auto-focus effect (line 160-164) now checks `targetAdapter?.inputMode !== "type"` before focusing the Composer textarea. Type-mode terminals (shell, lazygit, tmux) no longer have their xterm focus stolen by the Composer.
- **Step 4**: Post-submit focus restore (line 279) now skips `textarea.focus()` for type-mode terminals, letting xterm retain DOM focus.

### src/stores/projectStore.ts
- **Step 2**: `removeTerminal` now detects when the deleted terminal was focused and transfers focus to the adjacent terminal in the same worktree (next preferred, then previous). If no terminals remain in the worktree, focus is cleared entirely. Non-focused terminal deletion is unaffected.

### src/terminal/TerminalTile.tsx
- **Step 3**: Added a `useEffect` that gives xterm DOM focus when `terminal.focused` becomes true for type-mode terminals. This ensures keyboard shortcut navigation (next/prev terminal) properly transfers DOM focus so keystrokes reach the PTY immediately. Paste-mode terminals are unaffected (Composer keeps focus).

## Edge Cases Handled
- paste/bracketed-paste mode auto-focus behavior unchanged (Composer textarea still gets focus)
- Deleting a non-focused terminal does not affect current focus
- Deleting the only terminal in a worktree clears focus without crash
- Keyboard switching to a paste-mode terminal still correctly focuses the Composer (via the existing ComposerBar auto-focus effect, which continues to run for non-type modes)

## Tests
- All 22 test files fail with pre-existing ESM/CJS import configuration errors (not related to these changes)
- TypeScript compilation passes with zero errors (`npx tsc --noEmit`)

## Unresolved Problems
- None related to this task
- Pre-existing: vitest test infrastructure is broken (ESM import issues across all test files)
