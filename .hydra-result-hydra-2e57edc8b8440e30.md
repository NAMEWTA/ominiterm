Files changed and why

- `skills/.claude-plugin/plugin.json`: added the TermCanvas Claude plugin manifest.
- `skills/skills/hydra/SKILL.md`: copied the existing Hydra skill into the new plugin skill layout.
- `src/App.tsx`: added `window.__tcApi.setCustomTitle()` for renderer-side terminal title updates.
- `electron/api-server.ts`: added `PUT /terminal/:id/custom-title` and enabled PUT body parsing.
- `cli/termcanvas.ts`: added `terminal set-title <id> <title>` and updated CLI help text.
- `electron/skill-manager.ts`: replaced the old Hydra-only installer with plugin registration plus Codex symlink management.
- `electron/main.ts`: switched startup/install wiring from `hydra-skill` to `skill-manager` and passed `app.getVersion()`.
- `electron-builder.yml`: changed extraResources from `hydra/skill` to the new `skills` directory.
- `skills/skills/rename/SKILL.md`: added the rename skill for `/termcanvas:rename`.
- Removed `electron/hydra-skill.ts`, `hydra/skill/SKILL.md`, and the empty `hydra/skill/` directory as part of the migration.

Issues found

- `npx vite build` fails in this worktree during the Electron build with `Rollup failed to resolve import "adm-zip" from electron/main.ts`.
- Because the updated worktree app could not be started in dev mode, full end-to-end manual verification of `terminal set-title` against the new route could not be completed.
- I did attempt the manual CLI check against the already running TermCanvas instance; `terminal list` worked, but `terminal set-title` returned `Not found`, which is consistent with that existing app instance not including this branch's new API route.

Whether tests pass

- `npx tsc --noEmit`: passed.
- `npx vite build`: failed with the existing `adm-zip` resolution error above.

Any unresolved problems

- End-to-end manual verification of terminal renaming remains blocked until the Electron/Vite build can resolve `adm-zip` and the updated app can be launched.
